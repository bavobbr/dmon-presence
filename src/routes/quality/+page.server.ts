import type {PageServerLoad} from './$types';import {quality,selectedDataset} from '$lib/server/queries';export const load:PageServerLoad=({url})=>{const selected=selectedDataset(url.searchParams.get('dataset'));const dataset=(selected.all as any[]).find(d=>d.id===selected.id);return {datasetId:selected.id,dataset,rows:selected.id?quality(selected.id):[]}};
