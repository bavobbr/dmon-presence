import type {PageServerLoad} from './$types';import {monthly,selectedDataset} from '$lib/server/queries';export const load:PageServerLoad=({url})=>{const {id}=selectedDataset(url.searchParams.get('dataset'));return {datasetId:id,data:id?monthly(id):{players:[],months:[]}}};
