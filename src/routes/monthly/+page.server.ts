import type {PageServerLoad} from './$types';import {monthly,selectedDataset} from '$lib/server/queries';export const load:PageServerLoad=({url})=>{const {id,isAll}=selectedDataset(url.searchParams.get('dataset'));return {datasetId:id,isAll,data:id&&!isAll?monthly(id):{players:[],months:[]}}};
